# yaml-language-server: $schema=../../schemas/development.json

# ANCHOR: all
typescript:
  no_default_deps: true

  catalog: true

  package_presets:
    with_catalog:
      name: with_catalog
      package_json:
        devDependencies:
          # Named catalog
          svelte: catalog:svelte
          # Default catalog
          hono: "catalog:"
# ANCHOR_END: all
