[typescript]

[typescript.ts_config_presets.one]
extends_presets = ["two"]

[typescript.ts_config_presets.two]
extends_presets = ["three"]

[typescript.ts_config_presets.three]
extends_presets = ["one"]

[typescript.package_presets.circular_tsconfigs]
name = "circular_tsconfig"
package_json = "one"
dir = "packages/mypkg"
ts_config = [{ output = "tsconfig.abc.json", config = "one" }]

[typescript.package_json_presets.one]
