out_dir = "../output"

[typescript]

[typescript.tsconfig_presets.one]
extend_presets = ["two"]

[typescript.tsconfig_presets.two]
extend_presets = ["three"]

[typescript.tsconfig_presets.three]
extend_presets = ["one"]

[typescript.package_presets.circular_tsconfigs]
name = "circular_tsconfig"
package_json = "one"
dir = "packages/mypkg"
ts_config = [{ output = "tsconfig.abc.json", config = "one" }]

[typescript.package_json_presets.one]
