vars = { my_var = 15 }

templates = { lit_template = "my_var: {{ my_var }}" }

templates_dir = "../templates"

[typescript.package_presets.circular_package_json]
name = "circular_package_json"
package_json = "one"
dir = "packages/mypkg"

[typescript.package_json_presets.one]
extends_presets = ["two"]

[typescript.package_json_presets.two]
extends_presets = ["three"]

[typescript.package_json_presets.three]
extends_presets = ["one"]
