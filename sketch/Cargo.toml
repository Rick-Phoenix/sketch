[package]
name = "sketch-it"
version = "0.2.0"

description = "🖌️ Templating made simple. A tool to define and generate files and reusable project structures"
categories = ["command-line-utilities", "development-tools"]
keywords = ["boilerplate", "generation", "templating"]
repository = "https://github.com/Rick-Phoenix/sketch"

edition.workspace = true
license.workspace = true
authors.workspace = true

readme = true

[[bin]]
name = "sketch"
path = "src/main.rs"

[package.metadata.release]
tag-message = "chore: Release version {{version}}"
tag-name = "v{{version}}"
allow-branch = ["main"]
pre-release-hook = ["bash", "-c", "cd .. && ./pre_release.sh {{version}}"]

[dev-dependencies]
pretty_assertions = "1.4.1"
assert_cmd = "2.0.17"
clap-markdown = "0.1.5"
indoc = "2.0.6"

[dependencies]
globset = "0.4.16"
walkdir = "2.5.0"
semver = { version = "1.0.27" }
schemars = { version = "1.0.4", features = ["indexmap2"] }
askama = { workspace = true }
toml = { version = "0.9.5", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
clap = { version = "4.5.46", features = ["derive"] }
thiserror = { workspace = true }
reqwest = { version = "0.12.23", features = ["json"] }
tokio = { version = "1", features = ["full"] }
url = "2.5.7"
futures = "0.3.31"
serde_yaml_ng = "0.10.0"
convert_case = { workspace = true }
regex = { workspace = true }
maplit = { workspace = true }
paste = { workspace = true }
merge = "0.2.0"
tera = "1.20.0"
indexmap = { version = "2.11.0", features = ["serde"] }
uuid = { version = "1.18.1", features = ["v4"] }

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
